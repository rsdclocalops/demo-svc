{
  "dependencies": {
    "s3": {"buckets":[
      {
        "id": "attachments",
        "prefix": "attachments",
        "exports": {
          "ATTACHMENTS_BUCKET_ARN": "$arn",
          "ATTACHMENTS_BUCKET_NAME": "$name"
        },
        "skip_preview": false,
        "preview_only": false
      }
    ]}
  }
}
